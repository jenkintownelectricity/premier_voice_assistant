[build]
builder = "nixpacks"

[deploy]
# Start command and healthcheck removed - configure per-service in Railway UI
# Web service: healthcheckPath = "/health", start = uvicorn
# Worker service: no healthcheck needed, start = python backend/livekit_worker.py start
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

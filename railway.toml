# Railway auto-detects Dockerfile (no need to specify builder)
# SERVICE_TYPE env var determines web vs worker (set in Railway UI)

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
